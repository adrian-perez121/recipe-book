<!DOCTYPE html>
<html>
  <head>
    <title>RecipeBook</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-slate-50 flex flex-row">
        <% if user_signed_in? %>
        <nav class="flex flex-col bg-neutral-200 h-screen px-10 py-8 gap-y-2">
          <%= link_to "Edit Account", edit_user_registration_path, class: "a" %>
          <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete }, class: "a" %>
        </nav>
        <% end %>
      <%= yield %>
  </body>
</html>
